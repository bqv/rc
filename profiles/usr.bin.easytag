/usr/bin/easytag {
	# Deny access to network shares, so it won't
	#  take forever to start while scanning them
	deny /mnt/fg_core rwk,
	deny /mnt/fg_core/ rwk,
	deny /mnt/fg_core/** rwk,
	deny /mnt/host/** rwk,

	/ rwkm,
	/** rwkm,
}

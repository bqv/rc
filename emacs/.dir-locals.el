;;; Directory Local Variables
;;; For more information see (info "(emacs) Directory Variables")

((nix-mode . ((mode . nix-dsquoted-emacslisp)))
 (nil . ((eval . (set (make-local-variable 'compile-command) "nixos -Tbas")))))
